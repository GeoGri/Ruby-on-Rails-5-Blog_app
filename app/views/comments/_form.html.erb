<%= form_for([@post, @post.comment.build]) do |f| %>
  <%= f.label :content, "Add your comment:" %>
  <%= f.text_field :content, placeholder: 'Your comment', class: "form-control" %>
  <%= f.submit "Add comment", class: "btn btn-primary", stle: "float: right;"%>
<% end %>